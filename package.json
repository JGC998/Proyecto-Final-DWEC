{
  "dependencies": {
    "chart.js": "^4.5.1",
    "react-chartjs-2": "^5.3.1",
    "react-router-dom": "^7.13.0"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  },
  "scripts": {
    "front": "npm run dev --prefix SoundRate",
    "back": "cd api && npx json-server --watch db.json --port 3000",
    "start:all": "concurrently \"npm run back\" \"npm run front\""
  }
}